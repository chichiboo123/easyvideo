# EasyVideo UI/UX 문제점 분석 및 개선 계획 (2026-05-15)

## 1) 분석 범위
- 코드 기반으로 현재 편집 흐름(툴바, 타임라인, 속성 패널, 미리보기, 내보내기)을 점검했다.
- 대상 사용자(초3~6) 기준의 사용성 + 범용 영상편집기 공통 기능 누락 여부를 함께 평가했다.

## 2) 현재 UX 강점
1. **학습 부담이 낮은 구조**
   - 툴바 기능이 `선택/분할/삭제/내보내기` 중심으로 단순해 첫 진입 장벽이 낮다.
2. **키보드 단축키 제공**
   - 스페이스 재생/정지, `S` 분할, Delete 삭제, `+/-` 줌 지원으로 숙련 사용자의 작업 속도를 높일 수 있다.
3. **트랙/상태 모델이 단순함**
   - 영상 배열 + 단일 오디오 + 자막/스티커 분리 구조로 유지보수와 학습이 쉽다.

## 3) 주요 UI/UX 문제점

### A. 실수 복구 경로 부재 (치명)
- 현재 상태 관리에 **히스토리(Undo/Redo) 계층이 전혀 없다.**
- 삭제/분할/재정렬/속성 변경 후 즉시 되돌릴 수 없어 초등 사용자에게 심리적 부담이 크다.
- 특히 키보드 Delete 오작동 시 즉시 복구가 불가능하다.

### B. 편집 단계 피드백 부족
- 앱 내 `step` 상태는 존재하지만, 현재 편집 완료도(예: 영상 추가 전 내보내기 비활성 이유, 자막 없는 상태 안내)를 맥락적으로 설명하는 인터랙션이 약하다.
- 결과적으로 “다음에 무엇을 해야 하는지”를 툴팁/빈 상태 화면에서 더 명확히 알려줄 필요가 있다.

### C. 선택/삭제의 안전장치 부족
- 삭제는 바로 반영되며 확인 UI 또는 스낵바(되돌리기 CTA)가 없다.
- 아동 대상에서는 “실수해도 괜찮다”는 복구 affordance가 매우 중요하다.

### D. 타임라인 정밀 편집 장치 부족
- 분할은 재생헤드 기준 1회 동작 중심이며, 범용 편집기에서 흔한 `클립 가장자리 드래그 트리밍`이 없다.
- 현재는 duration 중심 모델이라 트리밍 시작점(인점/아웃점) 조정의 표현력이 약하다.

## 4) 범용 영상편집기 대비 누락 기능 (핵심)

## ✅ 우선 추가 대상: **Undo / Redo (실행 취소/다시 실행)**

범용 편집기(모바일/데스크톱 공통)에서 사실상 기본인 기능이며, 현재 EasyVideo에 없다.
초등 사용자 UX에서도 “복구 가능성”은 편집 지속률을 결정하는 핵심 요소다.

### 왜 1순위인가
- 학습 비용 대비 체감효과가 가장 큼.
- 삭제/분할/재정렬/자막 수정 등 **모든 편집 액션의 안전망**이 된다.
- 향후 고급 기능(트랜지션, 트리밍, 멀티트랙) 추가 전 필수 기반이다.

## 5) 개선 실행 계획 (구현 로드맵)

### Phase 1. 히스토리 엔진 도입 (1주)
1. Zustand 스토어에 `past/present/future` 또는 `history[] + pointer` 구조 추가.
2. 히스토리 기록 대상 정의
   - videoClips, audioClip, captions, stickers, selection, currentTime(선택적).
3. 고빈도 이벤트 최적화
   - 드래그/슬라이더는 `debounce` 또는 `interaction end` 시점에만 커밋.
4. 메모리 보호
   - 스냅샷 최대 50~100개 제한 + 초과 시 FIFO 제거.

### Phase 2. UI 연결 (2~3일)
1. 툴바에 `되돌리기/다시실행` 버튼 추가.
2. 단축키 추가
   - Win/Linux: `Ctrl+Z`, `Ctrl+Shift+Z`
   - macOS: `⌘Z`, `⌘⇧Z`
3. 비활성 상태/툴팁
   - 히스토리 없으면 버튼 disabled + “되돌릴 작업이 없어요”.

### Phase 3. 아동 친화 UX 안전망 (2~3일)
1. 삭제 후 하단 스낵바: “삭제했어요. 되돌릴까요?” + `되돌리기` CTA.
2. 첫 사용 가이드(마스코트): “실수해도 되돌릴 수 있어요!” 노출.
3. 오류 예방 카피 정리: 부정문 최소화, 짧은 문장 중심.

### Phase 4. 회귀 테스트/품질 (2~3일)
1. 액션별 Undo/Redo 시나리오 테스트
   - 분할→삭제→되돌리기→다시실행
   - 자막 텍스트 변경 3회 후 단계별 되돌리기
2. FFmpeg export 전/후 상태 일관성 점검.
3. 키보드 이벤트 충돌(한글 IME 입력 중 단축키 오작동) 점검.

## 6) 후속 기능 백로그 (범용 편집기 정합)
Undo/Redo 이후 우선순위 제안:
1. **클립 트리밍(인/아웃점 드래그)**
2. 클립 복제(Duplicate)
3. 스냅/정렬 가이드(재생헤드 근접 스냅)
4. 오디오 페이드 인/아웃
5. 간단 전환(컷/페이드)

## 7) 성공 지표 (측정 가능)
- 편집 중 이탈률 감소
- 삭제 직후 Undo 사용률
- 첫 영상 완성까지 평균 시간 단축
- “실수 복구 가능” 관련 사용자 만족도 상승

## 8) 결론
EasyVideo는 이미 단순한 구조와 기본 단축키를 갖춘 좋은 출발점이다.
다만 아동 친화 편집기의 핵심은 “실수 복구 가능성”이며, 이를 위해 **Undo/Redo를 최우선 기능으로 추가**해야 한다.
해당 기능은 UX 안정성과 향후 기능 확장성 모두를 동시에 개선하는 가장 비용 효율적인 투자다.
