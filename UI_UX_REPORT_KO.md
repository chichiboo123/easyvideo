# EasyVideo UI/UX 종합 검토 및 개선 보고서 (2026-05-26)

## 1. 검토 범위
- 코드 베이스 전체(툴바·미디어 패널·프리뷰·속성 패널·타임라인·내보내기 모달)를 사용자 흐름 기준으로 점검.
- 대상 사용자(초3~6) + 일반 영상 편집 사용자 두 관점을 함께 적용.
- OpenCut(MIT) 기능셋과 CapCut/Davinci 등 인기 편집기의 기본기를 비교 기준선으로 사용.

---

## 2. 사용자 입장에서 불편한 UI/UX 문제점

### 🅐 자막 편집 표현력 부족 (가장 큰 불편)
1. **색상이 6종 프리셋만 제공** → 캐릭터/브랜드 색을 맞출 수 없음.
2. **폰트가 12개로 고정** → 어린이가 좋아하는 손글씨/만화체/팝업체 부족.
3. **굵기/기울임/밑줄/취소선 없음** → 강조 표현 불가.
4. **외곽선(스트로크)·그림자 미지원** → 밝은 배경에서 텍스트 가독성 떨어짐.
5. **정렬/줄 간격/자간 미지원** → 두 줄 자막을 깔끔하게 만들 수 없음.
6. **자막 회전 불가** → 강조 디자인이 단조로움.
7. **배경 패딩/모서리 둥글기 조절 불가** → 말풍선 형태 표현 어려움.
8. **자막 SRT/VTT 가져오기·내보내기 없음** → 외부 자막 활용 불가.
9. **자막 일괄 편집/복제 없음** → 같은 스타일 자막을 반복 입력해야 함.
10. **재생 위치에서 분할/시간 미세조정 도구 없음** → 시간 입력만 가능.
11. **애니메이션이 fade in/out 2종 뿐** → 캡컷처럼 슬라이드/팝/타자기 효과 부재.
12. **자막용 입력은 단순 텍스트박스** → 줄바꿈 전후 미리보기가 어려움.

### 🅑 타임라인의 혼란 요소
1. **V2 트랙이 짝/홀수 인덱스로 분리됨** → 사용자가 의도하지 않은 분할이 발생.
2. **M2 트랙이 placeholder만 보임** → 비활성 트랙이 시각적 잡음.
3. **클립의 시작 위치(인점) 트림 불가** → 좌측 핸들이 duration만 변경, 실제 in/out 컷이 안 됨.
4. **드래그로 시간축 이동 불가** → 클립 순서 재정렬만 가능.
5. **재생헤드·클립 경계 스냅 부재** → 정밀 컷 작업이 어려움.
6. **마커/북마크 부재** → 긴 영상에서 작업 지점 표시가 안 됨.
7. **타임라인 비어있을 때도 눈금이 보이지 않음** → 초기 상태가 빈 화면처럼 보임.
8. **수직 스크롤이 통일되지 않음** → 트랙이 많아지면 가독성 떨어짐.

### 🅒 프리뷰 / 재생 제어
1. **전체화면 모드 없음** → 결과 확인이 답답함.
2. **종횡비 선택 없음** → 16:9 외 YouTube Shorts(9:16)/인스타(1:1, 4:5) 미지원.
3. **프레임 단위 이동 키 없음** → 정밀 컷 어려움.
4. **방향키 시킹 없음** → ←/→로 5초 이동을 지원해야 함.
5. **오버레이 핸들 부재** → 모서리 핸들로 크기·회전 불가, 위치 드래그만 가능.
6. **여러 자막 겹쳤을 때 z-index/선택 어려움**.

### 🅓 도구바·전반적 동작
1. **새 프로젝트/프로젝트 불러오기 부재** → 잘못 만든 작업을 한 번에 초기화 못 함.
2. **단축키 안내 부재** → 키보드 사용자가 단축키를 알기 어려움.
3. **삭제 시 토스트(되돌리기 CTA) 없음** → 초등 사용자에게 안전망 약함.
4. **자동 저장 부재** → 새로고침 시 모든 작업이 사라짐.
5. **선택 도구/분할 도구가 토글이지만 분할은 1회 동작** → 멘탈 모델 혼란.

### 🅔 미디어 패널
1. **오디오 프리셋이 url 없는 가짜 데이터** → 클릭해도 실제 재생이 안 됨.
2. **이미지 탭에 드래그 영역 없음** → 일관성 부족.
3. **자막 텍스트가 작은 textarea** → 길게 작성하면 가려짐.
4. **자막 미리보기 없음** → 색·폰트 선택이 추측이 됨.

### 🅕 속성 패널
1. **속도 변경 버튼이 disabled** → 표시만 되고 작동 안 함.
2. **트림 in/out 입력 없음** → 클립의 일부만 사용하는 작업이 어려움.
3. **클립별 볼륨/페이드 없음** → 오디오 믹싱 불가.
4. **자막의 다양한 스타일을 한 곳에 펼침** → 옵션이 많아지면 패널이 길어짐(섹션화 필요).

### 🅖 내보내기
1. **품질/해상도 옵션 없음** → 가벼운 미리보기 내보내기가 불가.
2. **자막은 drawtext 한 줄로만 적용** → 굵기/외곽선/그림자/배경이 반영되지 않음.

---

## 3. OpenCut / CapCut 대비 기능 격차

| 기능 | OpenCut | CapCut | 현재 EasyVideo | 개선 후 |
|---|:-:|:-:|:-:|:-:|
| 다중 비디오 트랙 | ◯ | ◯ | △(가짜) | ◯ |
| 다중 오디오 트랙 | ◯ | ◯ | △(슬롯만) | ◯ |
| 클립 분할 | ◯ | ◯ | ◯ | ◯ |
| 양쪽 트림(인/아웃) | ◯ | ◯ | △ | ◯ |
| 클립 속도 변경 | ◯ | ◯ | ✕(비활성) | ◯ |
| 클립별 볼륨 | ◯ | ◯ | ✕ | ◯ |
| 오디오 페이드 인/아웃 | ◯ | ◯ | ✕ | ◯ |
| 트랜지션(페이드/디졸브/슬라이드/와이프) | ◯ | ◯ | △(페이드만) | ◯ |
| 비디오 효과(흑백/빈티지/블러/비네트) | ◯ | ◯ | △(3개) | ◯ |
| 자막 스타일(굵기·기울임·외곽선·그림자) | ◯ | ◯ | ✕ | ◯ |
| 자막 애니메이션(슬라이드/팝/타자기) | △ | ◯ | △(페이드만) | ◯ |
| Google Fonts 다수 지원 | △ | ◯ | △(12개) | ◯(100+) |
| SRT 자막 가져오기/내보내기 | △ | ◯ | ✕ | ◯ |
| 종횡비 프리셋(16:9/9:16/1:1/4:5) | ◯ | ◯ | ✕ | ◯ |
| 마커/북마크 | △ | ◯ | ✕ | ◯ |
| 프로젝트 저장/불러오기 | ◯ | ◯ | △(저장만) | ◯ |
| 자동 저장 | △ | ◯ | ✕ | ◯ |
| 토스트 / 되돌리기 CTA | △ | ◯ | ✕ | ◯ |
| Undo/Redo + 단축키 | ◯ | ◯ | ◯ | ◯ |
| 단축키 도움말 | △ | ◯ | ✕ | ◯ |
| 음성 녹음(보이스 오버) | △ | ◯ | ✕ | ◯ |
| 키보드 시킹(←/→/J/K/L) | ◯ | ◯ | △ | ◯ |
| 전체화면 미리보기 | △ | ◯ | ✕ | ◯ |
| 스냅 / 정렬 가이드 | ◯ | ◯ | ✕ | ◯ |
| 클립 복제 | ◯ | ◯ | ✕ | ◯ |

---

## 4. 실행한 개선 (이번 PR 범위)

### 4.1 자막 편집 고도화
- `Caption` 타입을 23개 속성으로 확장: weight, italic, underline, strikethrough, align, lineHeight, letterSpacing, stroke(color/width), shadow(color/blur/offsetX/Y), rotation, padding, borderRadius, animationIn/Out 8종.
- `lib/fonts.ts`: 한국어 24종 + 라틴 78종 = **총 102종 Google Fonts** 일괄 프리로드.
- `PropertiesPanel`을 섹션형(텍스트/모양/효과/타이밍/위치/애니메이션)으로 재구성.
- 자막 텍스트 입력은 큰 textarea + 라이브 미리보기.
- 자막 정렬(좌·중·우), 자간, 줄간격, 회전 슬라이더 추가.
- 외곽선(stroke) 색/굵기, 그림자(shadow) 색/번짐/오프셋 추가.
- 9종 등장/퇴장 애니메이션: none, fade, slide-up/down/left/right, zoom-in/out, bounce, pop, typewriter.
- 자막 복제(duplicate) 버튼.
- SRT 가져오기/내보내기 버튼.
- 빠른 위치 프리셋(상단/중앙/하단 × 좌/중/우).
- 텍스트 프리셋 7종(기본/팝/네온/그라데이션/외곽선/그림자/타이틀).

### 4.2 OpenCut/CapCut 기능 흡수
- **클립 속도**: 0.25× ~ 4× (Properties Panel 슬라이더, FFmpeg `setpts`/`atempo` 적용).
- **클립 볼륨**: 0~200% (per clip 볼륨).
- **클립 페이드 인/아웃**: 비디오 + 오디오 모두 지원.
- **양쪽 트림(in/out)**: 좌측 핸들이 실제 in-point를 이동.
- **종횡비 프리셋**: 16:9 / 9:16 / 1:1 / 4:5 / 자유.
- **트랜지션 5종**: cut / fade / dissolve / slide-left / wipe-up.
- **비디오 효과 8종**: none / vintage / bright / bw / warm / cool / blur / vignette.
- **마커**: 타임라인에 시간/라벨/색 마커 추가, 클릭으로 시킹.
- **스냅**: 재생헤드/클립 경계에 1초 미만 거리에서 스냅.
- **클립 복제**.
- **다중 오디오 트랙**: BGM + 효과음 + 보이스오버.
- **보이스 오버 녹음**: 브라우저 MediaRecorder로 마이크 녹음 후 오디오 클립 추가.
- **프로젝트 저장/불러오기**: JSON 다운로드 + 업로드 시 상태 복원.
- **자동 저장**: 5초마다 localStorage에 저장, 새로고침 시 복원 다이얼로그.

### 4.3 UX 안전망 & 안내
- **토스트 시스템**: 삭제 시 "되돌리기" CTA 토스트 4초 노출.
- **단축키 도움말 모달**: `?` 또는 툴바 버튼.
- **방향키 시킹**: ←/→ 1초, Shift 누르면 5초.
- **J/K/L 시킹**: J=뒤로 5초, K=일시정지, L=앞으로 5초.
- **전체화면 미리보기**: F 키 또는 버튼.
- **빈 상태 안내 강화**: 각 패널의 첫 단계 가이드.
- **삭제 확인 카피**: "되돌릴 수 있어요" 안내 추가.

### 4.4 내보내기 강화
- 해상도 프리셋: 720p / 1080p / 원본.
- 자막을 ASS subtitle 필터로 적용 → 굵기·외곽선·그림자·정렬·폰트 보존.
- 클립 속도(`setpts`/`atempo`) 반영.
- 클립 볼륨/페이드 반영.
- 종횡비에 따라 영상 크기 자동 변환.

### 4.5 코드 품질
- 사용하지 않는 `SimpleTimeline`/`StepBar`/`MascotHelper`/`CaptionEditor`/`PreviewPanel`/`StickerPicker`/`VideoDropZone`/`ExportButton`은 신규 컴포넌트로 대체 후 보존(orphan). 추후 정리 가능.

---

## 5. 성공 지표 (측정 가능)
- 자막 추가 후 “기본 스타일 그대로” 비율 ↓ (스타일 변경률 ↑)
- 종횡비 변경 사용률 (Shorts/Reels 워크플로 검증)
- 자동 저장 복원 다이얼로그 동의율
- Undo/Redo 사용률 (삭제 후 즉시 되돌리기 CTA 클릭률)
- 단축키 모달 1회 이상 노출 사용자 비율
- 첫 영상 완성까지 평균 시간 단축

---

## 6. 향후 백로그 (이번 PR 범위 밖)
1. 키프레임 애니메이션(위치/크기/회전 시간선)
2. 오디오 파형(waveform) 실제 렌더링
3. 멀티셀렉트 + 일괄 편집
4. 그린 스크린(크로마 키)
5. 모션 트래킹(브라우저 한계로 보류)
6. PWA 오프라인 사용
7. 다국어(영어) UI 토글
